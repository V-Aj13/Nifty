import React from 'react';
import { View, Text } from 'react-native';

function aProposScreen(props) {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
          <Text>on s'amuse beaucoup </Text>
        </View>
      );
}

export default aProposScreen;